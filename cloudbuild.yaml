steps:
  # Docker Build
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 
           'us-west2-docker.pkg.dev/${PROJECT_ID}/art-bird/clumsybird-image', 
           '.']

  # Docker Push
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 
           'us-west2-docker.pkg.dev/${PROJECT_ID}/art-bird/clumsybird-image']
